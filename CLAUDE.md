## Arena Cloud Code Rules

- All product changes must consider both Chinese and English versions.
- Supported UI/product languages are only `zh` and `en`.
- Do not add new hardcoded user-facing Chinese or English strings; add or reuse i18n keys for both languages.
- Local `/Users/xuan/Workspace/arena` is only for browser control, inspection, notes, and testing convenience.
- Write code, run builds, run app commands, and deploy in the remote canonical workspace: `/home/xuan/docker/arena` on `xuan@192.168.43.5`.
- Do not overwrite the remote workspace from local unless the user explicitly asks and the local tree has just been verified as the intended source.

@AGENTS.md
